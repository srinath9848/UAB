@using UAB.enums
@model List<UAB.DTO.DashboardDTO>
@{
    ViewData["Title"] = "QA Summary";
}

<style>
    .availableCharts, .coderRebuttalCharts, .shadowQaRebuttalCharts {
        width: 60px;
    }
</style>
<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Projects</h4>
                    @await Html.PartialAsync("_Msg")
                </div>
                <table class="table table-striped table-bordered dataTable">
                    <thead>
                        <tr>
                            <th scope="col" class="tblHeaderRow">
                                Name
                            </th>
                            <th scope="col" class="tblHeaderRow">
                                Available Charts
                            </th>
                            <th scope="col" class="tblHeaderRow">
                                Coder Rebuttal  Charts
                            </th>
                            <th scope="col" class="tblHeaderRow">
                                Shadow QA Rejected Charts
                            </th>
                        </tr>
                    </thead>
                    <tbody style="cursor:pointer;">
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td scope="col">
                                    <b> @item.ProjectName</b>
                                </td>
                                <td scope="col" style="text-align:center">
                                    @if (item.AvailableCharts == 0)
                                    {
                                    <label class="availableCharts">@item.AvailableCharts</label>
                                        <b>&nbsp;&nbsp; &nbsp; &nbsp;</b>
                                        <a href="@Url.Action("GetQAAvailableChart", "UAB")" style="pointer-events: none;" class="btn btn-info btn-sm">Get Next</a>
                                    }
                                    else
                                    {
                                <label class="availableCharts">@item.AvailableCharts</label>
                                        <b>&nbsp; &nbsp; &nbsp; &nbsp;</b>
                                        <a href="@Url.Action("GetQAAvailableChart", "UAB",new { Role=Roles.QA,ChartType="Available",ProjectID = item.ProjectID})" class="btn btn-info btn-sm">Get Next</a>
                                    }
                                </td>
                                <td scope="col" style="text-align:center">
                                    @if (item.CoderRebuttalCharts == 0)
                                    {
                                    <label class="coderRebuttalCharts">@item.CoderRebuttalCharts</label>
                                        <b>&nbsp;&nbsp; &nbsp; &nbsp;</b>
                                        <a href="@Url.Action("GetQARebuttalChartsOfCoder", "UAB")" style="pointer-events: none;" class="btn btn-info btn-sm">Get Next</a>
                                    }
                                    else
                                    {
                                <label class="coderRebuttalCharts">@item.CoderRebuttalCharts</label>
                                        <b>&nbsp; &nbsp; &nbsp; &nbsp;</b>
                                        <a href="@Url.Action("GetQARebuttalChartsOfCoder", "UAB",new { Role=Roles.QA,ChartType="RebuttalOfCoder",ProjectID = item.ProjectID})" class="btn btn-info btn-sm">Get Next</a>
                                    }
                                </td>
                                <td scope="col" style="text-align:center">
                                    @if (item.ShadowQARebuttalCharts == 0)
                                    {
                                    <label class="shadowQaRebuttalCharts">@item.ShadowQARebuttalCharts</label>
                                        <b>&nbsp;&nbsp; &nbsp; &nbsp;</b>
                                        <a href="@Url.Action("GetQARejectedChartsOfShadowQA", "UAB")" style="pointer-events: none;" class="btn btn-info btn-sm">Get Next</a>
                                    }
                                    else
                                    {
                                <label class="shadowQaRebuttalCharts">@item.ShadowQARebuttalCharts</label>
                                        <b>&nbsp; &nbsp; &nbsp; &nbsp;</b>
                                        <a href="@Url.Action("GetQARejectedChartsOfShadowQA", "UAB",new { Role=Roles.QA,ChartType="ShadowQARejected",ProjectID = item.ProjectID})" class="btn btn-info btn-sm">Get Next</a>
                                    }
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
<script src="~/dashboardassets/libs/jquery/dist/jquery.min.js"></script>
<script type="text/javascript">
    $(document).ready(function () {

        $("#qa").addClass('selected');
       
    });
</script>


